# dgrefhelp-tutorials

Public-facing GP trainee tutorials built around case-based staged reveal teaching.

## Local dev

```bash
npm install
npm run dev
```

Then open http://localhost:3000

## Learning notes

- Next.js App Router lives in `src/app/`
- Each folder with a `page.tsx` becomes a route.
- `layout.tsx` wraps all pages.
